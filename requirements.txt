# Core DL Framework (Install manually for specific CUDA version if needed)
torch==2.2.1
torchvision==0.17.1
torchaudio==2.2.1

# Core Utilities
packaging
ninja
PyYAML
omegaconf
protobuf>=3.19.0,<4.0.0
tqdm
scikit-learn

# Acceleration & Optimization
accelerate
flash-attn>=2.0.0  # Note: Use MAX_JOBS=256 and --no-build-isolation when installing

# Vision & Image Processing
opencv-python
imageio
imageio-ffmpeg
matplotlib

# Metrics & Logging
lpips
tensorboard
wandb

# Model Libraries (Implied by DiT/Transformer usage in paper)
transformers
diffusers
huggingface_hub

# Other tools
gdown